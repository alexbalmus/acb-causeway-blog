CREATE TABLE blog.Blog (id BIGINT NOT NULL, handle VARCHAR(40) NOT NULL, name VARCHAR(40) NOT NULL, version INTEGER NOT NULL, PRIMARY KEY (id))
CREATE TABLE blog.Post (id BIGINT NOT NULL, content VARCHAR(4000) NOT NULL, title VARCHAR(40) NOT NULL, version INTEGER NOT NULL, blog_id BIGINT, PRIMARY KEY (id))
ALTER TABLE blog.Blog ADD CONSTRAINT Blog__name__UNQ UNIQUE (name)
ALTER TABLE blog.Post ADD CONSTRAINT Post__title__UNQ UNIQUE (title)
ALTER TABLE blog.Post ADD CONSTRAINT FK_Post_blog_id FOREIGN KEY (blog_id) REFERENCES blog.Blog (id)
CREATE TABLE SEQUENCE (SEQ_NAME VARCHAR(50) NOT NULL, SEQ_COUNT NUMERIC(38), PRIMARY KEY (SEQ_NAME))
INSERT INTO SEQUENCE(SEQ_NAME, SEQ_COUNT) values ('SEQ_GEN', 0)
